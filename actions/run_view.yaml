---
  name: "View"
  runner_type: "python-script"
  description: "View recovery plan configuration / List recovery plans"
  enabled: true
  entry_point: "run_view.py"
  parameters:
    plan_name:
      type: "string"
      description: "Specify recovery plan name to view its configuration or * to show available recovery plans"
      required: true
      default: "*"
